const prompt = require("prompt");
const BOARD = require("./board");
const Robot = require("./robot");
const { startPrompt, InputPrompt, processInput, ROBOTS, ACTIVE_ROBOT } = require("./input");

startPrompt();